volumes:
  data:
    driver: local
  logs:
    driver: local

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      APP_HOST: 0.0.0.0
    volumes:
      - data:/app/data
      - logs:/app/logs